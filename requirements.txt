# Runtime dependencies for TrafficSight.
# Pin to versions known to work together (July 2025).

PyQt6>=6.6
numpy>=1.24
opencv-python>=4.8
psycopg2-binary>=2.9
filterpy>=1.4.5
ultralytics>=8.1

# Optional but recommended:
# torch>=2.1            # auto-installed by ultralytics if missing
# torchvision>=0.16     # same
